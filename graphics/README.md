EQ12 Graphics Module

This folder contains a small Simplified-inspired graphics generator used to create promotional images for deal alerts.

Usage:
- `python -m graphics.graphics_alert --in deals.json --out out.png` (or use the PowerShell wrapper `Get-DealGraphic.ps1`).
